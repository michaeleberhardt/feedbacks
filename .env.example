# ==============================================
# Feedbacks Application - Docker Configuration
# ==============================================
# Copy this file to .env and modify as needed
# Usage: docker-compose up -d

# ==============================================
# Backend Configuration
# ==============================================

# Port to expose the backend API (default: 3001)
BACKEND_PORT=3001

# JWT Secret for authentication
# IMPORTANT: Change this to a strong, random string in production!
# Generate one with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-strong-secret

# ==============================================
# Frontend Configuration
# ==============================================

# Port to expose the frontend (default: 80)
FRONTEND_PORT=80

# Backend API URL that the frontend will use
# This is baked into the frontend at build time
# For local Docker: http://localhost:3001
# For production with domain: https://api.yourdomain.com
VITE_API_URL=http://localhost:3001

# ==============================================
# SMTP Configuration (set via admin UI)
# ==============================================
# SMTP settings are stored in the database and configured
# through the Settings page in the admin UI.
# The following are just examples of what you'll configure:
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=false
# SMTP_TLS_REJECT=true
#
# APP_URL=http://localhost (for survey links in emails)
